apiVersion: v1
kind: PersistentVolume
metadata:
  name: mypv
  label: local
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  storage:
    capacity:
      storage: 10Gi
    hostPath:
       path: "/mnt/data"
 
